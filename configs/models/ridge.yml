model:
  name: ridge

study:
  name: ridge-optimisation
  storage: sqlite:///optuna.db

params:
  alpha: 1.0

optuna:
  enabled: true

  search_space:
    alpha:
      type: float
      low: 0.001
      high: 100
      log: true
